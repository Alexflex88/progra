function setup() {
  createCanvas(windowWidth, windowHeight);
  rectMode(CENTER)
  // put setup code here
}

function draw() {
  // put drawing code here
}

backround (0,0,0); //fond noir 

translate(width/2, height/2)
push();
rotate (millis)()/130;